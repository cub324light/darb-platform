import AkhtaiPage from "@/components/AkhtaiPage";

/* نفس وحدة «📚 أخطائي» — تفتح على قسم المحفوظات (روابط /review القديمة تبقى تعمل). */
export default function Page() {
  return <AkhtaiPage initialTab="saved" />;
}
