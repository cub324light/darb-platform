/* ═══════════ ترتيبُ الرحلة — خريطةُ الرحلة مكتوبةً مرّةً واحدة ═══════════
   ▸ المبدأ الذي تقوم عليه: الطالبُ لا يفتح درب ليتصفّح. يفتحه ومعه **سؤالٌ
     واحد**، وأوّلُ ما يراه يجب أن يكون جوابَ ذلك السؤال. وما لا يخدم سؤالَ
     مرحلته يتأخّر، وما يخصّ مرحلةً أخرى يبقى مخفياً كما هو.

   ▸ العطل الذي يُغلقه هذا الملف: كان ترتيبُ كلِّ شاشةٍ **ترتيبَ بنائها** لا
     ترتيبَ إلحاحها. فـ«مساري» تفتح على بطاقة إحصاءٍ ثم شبكةِ أربعِ بطاقات،
     وزرُّ «ابدأ المذاكرة» — وهو فعلُ الصفحة كلِّها — تحته. و«المدرسة» تفتح على
     التقويم والمواد قبل الواجب المستحقّ. و«أدوات الجامعة» ترتّب حاسباتها بترتيب
     كتابتها لا بترتيب فصل الطالب. و«المستقبل» تفتح خريجَ الجامعة على «التدريب
     التعاوني» — وهو بنصّه «قبل التخرّج».

   ▸ هذا الملفُّ **إعلانٌ لا تنفيذ**: لا يستورد مكوّناً ولا يلمس تخزيناً ولا
     يغيّر بوابةً ولا وجهة. الشاشاتُ تتبعه، واختبارٌ يُثبت أنها تتبعه فعلاً.
     ولا يضيف قدرةً ولا مرحلة: يسأل `PhaseView.allows` ولا يشتقّ شيئاً. */

import type { PhaseCapability } from "./transition";

/** ما نحتاجه من عرض المرحلة — سؤالٌ واحدٌ لا أكثر (بنيويّ، فلا دورة). */
export interface OrderView { allows: (cap: PhaseCapability) => boolean }

/* ════════════════ ١) شريطا التنقّل ════════════════ */

/** خاناتُ الشريط — **الوجهاتُ نفسُها لا تتغيّر**، والترتيبُ وحده يُقرَّر هنا. */
export type NavSlotId = "home" | "mid" | "focus" | "world" | "vault";

/**
 * ترتيبُ الشريطين — مصدرٌ واحدٌ يقرؤه السفليُّ والجانبيّ، فلا يتفرّقان ثانيةً.
 *
 * القاعدة: **المرساة ← جوابُ سؤال المرحلة ← فعلُ اليوم ← عالمُه ← سجلُّه.**
 *   · `home`  الرئيسية — المرساةُ التي يرجع إليها.
 *   · `mid`   العنصرُ المشتقُّ من القدرات (مساري · القبول · الأدوات · مهاراتي).
 *   · `focus` تركيز — فعلُ اليوم في كلّ مرحلة.
 *   · `world` المدرسة لمن له مقعدٌ فيها، و«المستقبل» لمن تجاوزها.
 *   · `vault` أخطائي — سجلٌّ يُرجَع إليه لا يُفتَح به يوم.
 *
 * والاستثناءُ الوحيد **خريجُ الجامعة**: سؤاله «كيف أصل إلى وظيفة؟» وجوابُه
 * «المستقبل» (تدريبٌ ووظائفُ وشهادات) لا «مهاراتي». فيتقدّم عليه — تبديلُ
 * موضعٍ بين عنصرين موجودين في الشريط أصلاً، بلا وجهةٍ جديدةٍ ولا بوابةٍ تُمسّ.
 */
export function navOrder(view: OrderView | null): NavSlotId[] {
  return view?.allows("career")
    ? ["home", "world", "focus", "mid", "vault"]
    : ["home", "mid", "focus", "world", "vault"];
}

/* ════════════════ ٢) الصفحة الرئيسية ════════════════ */

/**
 * أقسامُ الرئيسية لمن هو دون الجامعة (الجامعيُّ وخريجُها لهما لوحاتُهما).
 *   · `today`        «ماذا أفعل الآن؟» — البطلُ والفعلُ في كلّ الأحوال.
 *   · `signals`      «أين أنا من الرسميّ؟» — اختباراتُه ومواعيدُ الجهات.
 *   · `achievements` «هل أنا مستمرّ؟» — أسبوعُه وسلسلتُه.
 *
 * وأوّلُ ثانويٍّ وثانيه سؤالُهما «وش أذاكر اليوم؟»، فيأتي إيقاعُهما ثانياً
 * والرسميُّ ثالثاً — لا موعدَ قياسٍ يخصّهما بعد. أمّا مَن فُتح له بابُ القبول
 * (ثالثُ ثانويٍّ وخريجُ الثانوية) فسؤاله «وش وضعي للقبول؟»، وجوابُه في
 * `signals` — فيتقدّم على الإيقاع.
 */
export function homeSectionOrder(view: OrderView | null): string[] {
  return view?.allows("admission")
    ? ["today", "signals", "achievements"]
    : ["today", "achievements", "signals"];
}

/* ════════════════ ٣) مساري ════════════════ */

/**
 * الأكثرُ إلحاحاً أوّلاً · ثم ما يعين عليه · ثم الباقي.
 *   · `start`  فعلُ الصفحة و`CTA`ها الوحيد — وهو **ثابتٌ** فيبقى في الرأس.
 *   · `tiles`  ما يعين على البدء: اختبارُه · خطتُه · تقويمُه · مصادرُه.
 *   · `week`   حصيلةُ أسبوعه — قياسٌ بعد الفعل لا قبله.
 *   · `months` الأفقُ البعيد — آخرُ ما يحتاجه اليوم.
 */
export const ROADMAP_SECTION_ORDER = ["start", "tiles", "week", "months"] as const;

/* ════════════════ ٤) المدرسة ════════════════ */

/**
 * بالإلحاح الحقيقيّ لا بترتيب البناء: ما عليه اليوم وغداً، ثم ما يُقاس، ثم
 * ما يُحضَّر، ثم ما يُذكَر.
 *   `intro`        بطاقةٌ تُطوى بعد قراءتها — تُعرّف الصفحة ولا تنافس فعلاً.
 *   `homework`     مذكرةُ الواجبات — فعلُ الصفحة (المستحقُّ والمتأخّر).
 *   `tomorrow`     المطلوبُ غداً — أقربُ ضغطٍ زمنيّ (وبعد المذكرة لأنه يحيل إليها).
 *   `exams`        اختباراتُ المدرسة بمواعيدها.
 *   `timeline`     التقويمُ الدراسيّ — «كم باقٍ» على الاختبارات والإجازة.
 *   `subjects`     موادُّ فصله.
 *   `plan-link`    جدولُه.
 *   `requirements` · `projects`  ما يُحضَّر.
 *   `teachers`     مرجعٌ يُفتح عند الحاجة.
 *   `journal` · `memories`  لا إلحاحَ فيهما — ذاكرةُ سنةٍ لا مهمّةُ يوم.
 */
export const SCHOOL_SECTION_ORDER = [
  "intro", "homework", "tomorrow", "exams", "timeline", "subjects",
  "plan-link", "requirements", "projects", "teachers", "journal", "memories",
] as const;

/* ════════════════ ٥) أدوات الجامعة ════════════════ */

/**
 * بترتيب **فصلِ** الطالب لا بترتيب كتابة الحاسبات:
 *   `absence` الغياب — الخطرُ الوحيد الذي **لا يُعوَّض**: يتراكم أسبوعاً بأسبوع،
 *             ومَن بلغ سقفَ الحرمان لا حاسبةَ تُرجعه. فيُسأل عنه أوّلاً وطوال الفصل.
 *   `final`   الفاينل — «كم أحتاج في النهائي؟» سؤالُ آخر الفصل، وله فعلٌ ممكن.
 *   `gpa`     المعدل — **حصيلةٌ** تُحسب بعد النتائج، لا قرارٌ يُتّخذ قبلها.
 *   `convert` تحويل المعدل — يُحتاج عند تقديمٍ أو ابتعاثٍ فقط.
 */
export const UNI_TOOL_ORDER = ["absence", "final", "gpa", "convert"] as const;

/* ════════════════ ٦) المستقبل ════════════════ */

/**
 * بترتيب رحلة **خريج الجامعة**:
 *   `resume`        السيرة الذاتية — ورقتُه الأولى، ولا يتقدّم لشيءٍ بدونها.
 *   `opportunities` الفرص — التقديمُ نفسُه.
 *   `linkedin`      شبكتُه المهنية — تُبنى بالتوازي وتفتح ما لا يُعلَن.
 *   `training`      التدريبُ التعاونيّ — نصُّه «قبل التخرّج»، فهو لمن لم يتخرّج بعد.
 *   `grad`          الدراساتُ العليا — مسارٌ بديلٌ لا خطوةٌ تالية.
 *
 * ▓ كان `training` أوّلَ تبويبٍ و**التبويبَ المفتوحَ افتراضياً**، فيستقبل الخرّيجَ
 *   بما فات وقتُه. والتبويبُ الافتراضيُّ يتبع هذا الترتيب فلا يُكتب مرّتين.
 */
export const FUTURE_TAB_ORDER = ["resume", "opportunities", "linkedin", "training", "grad"] as const;

/* ════════════════ أداةٌ واحدة ════════════════ */

/** يرتّب عناصرَ لها `id` حسب ترتيبٍ معلَن — وما ليس فيه يبقى في ذيله بترتيبه. */
export function orderBy<T extends { id: string }>(items: T[], order: readonly string[]): T[] {
  const rank = new Map(order.map((id, i) => [id, i]));
  return [...items].sort((a, b) =>
    (rank.get(a.id) ?? order.length) - (rank.get(b.id) ?? order.length));
}
